# Supabase PostgreSQL session pooler connection
# Copy this file to .env and replace the placeholder values.

DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# Use 'database' for PostgreSQL only, or 'both' for PostgreSQL plus JSON/CSV backups.
STORAGE_MODE=both
